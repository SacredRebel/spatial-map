{
  "name": "spatial-map",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "The walkable world — a browser-native open world over real 1 m terrain, one world per community.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "node tests/world.test.mjs"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.5",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "playwright": "^1.63.0",
    "typescript": "~5.6.3",
    "vite": "^5.4.11"
  }
}
